# - name: gether facts
#   hosts: localhost
#   connection: local

#   tasks:
#   - name: gethering facts
#     ansible.builtin.debug:
#       msg: "{{ ansible_facts }}"

- name: gathering facts
  hosts: localhost
  connection: local

  
  tasks:
  - name: print the facts
    ansible.builtin.debug:
      msg: " {{ ansible_facts }} "