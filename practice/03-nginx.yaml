- name: nginx installation
  hosts: local
  connection: local
  tasks:
  - name: install nginx
    ansible.builtin.package: 
      name: nginx
      state: present
  - name: restart nginx
    ansible.builtin.service:
      name: nginx
      state: stopped
  become: yes